# Getting Roll Data

console.log(canvas.tokens.controlled[0].actor.getRollData());

# Finding Effect Keys

console.log(canvas.tokens.controlled[0].actor.system);

# Data Models

All data models are available under `dnd5e.dataModels` so you don't have to get the prototype of an existing instance...